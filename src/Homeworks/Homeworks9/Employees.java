package Homeworks.Homeworks9;
/*public class Employees extends Person {

    double salary;

    public Employees(String name, int age, String gender, double salary) {
        super(name, age, gender);
    }

    public boolean isSameName(Employees employee) {
        this.name.equals(employee.name);
        return false;
    }

    public String isSameAge(Employees employees) {
        if (this.age == employees.age) {
            return this.name + " and " + employees.name + " has same age ";
        } else {
            return this.name + " and " + employees.name + " has different age ";
        }
     public String isSameSalary (Employees employees) {
        if (this.salary == employees.salary) {
            return this.name + " and " + employees.name + " has compare salary";

        }
    }


    }
}

*/

//Необходимо создать класс Employee с полями как у Homeworks.Homeworks9.Person (из предыдущего задания) и поле зарплата.
// Класс должен иметь метод isSameName(Employee employee) который возвращает true,
// если у сотрудника у которого был вызван метод и сотрудника который был передан как параметр,
// одинаковое имя.




